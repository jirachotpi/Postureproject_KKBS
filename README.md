# 🤖 AI Posture Corrector (YOLOv11)

โปรแกรมตรวจจับท่านั่งผิดวิธี (Office Syndrome) ด้วย AI
แจ้งเตือนทันทีเมื่อ **คอยื่น (Text Neck)** หรือ **หลังค่อม (Hunchback)**

## 📂 ไฟล์ในโปรเจกต์
- `yolo_app.py`: **(แนะนำ)** เวอร์ชันล่าสุด ใช้ YOLOv11 ตรวจจับด้านข้าง แม่นยำสูง
- `app.py`: เวอร์ชันเก่า (ใช้ MediaPipe)
- `yolo11n-pose.pt`: โมเดล AI ที่ต้องใช้

## 🛠️ วิธีติดตั้ง (Installation)

1. **Clone โปรเจกต์นี้** หรือดาวน์โหลดเป็น ZIP แล้วแตกไฟล์
2. **ติดตั้ง Library** ผ่าน Terminal:
   ```bash
   pip install -r requirements.txt