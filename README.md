# 🤖 AI Posture Detection

โปรแกรมตรวจจับท่านั่งผิดวิธี จากด้านข้างด้วย AI เพื่อป้องกันอาการ Office Syndrome 
แจ้งเตือนทันทีเมื่อ **คอยื่น (Text Neck)** หรือ **หลังค่อม (Hunchback)** หรือ **นั่งนาน**

## 📂 ไฟล์ในโปรเจกต์
- `yolo_app.py`: เวอร์ชันล่าสุด ใช้ YOLOv11 ตรวจจับด้านข้าง แม่นยำสูง
- `app.py`: เวอร์ชัน MediaPipe
- `new_app.py`: เวอร์ชัน MediaPipe ที่ดีขึ้น
- `yolo11n-pose.pt`: โมเดล AI ที่ต้องใช้

## 🛠️ วิธีติดตั้ง (Installation)

1. **Clone โปรเจกต์นี้** หรือดาวน์โหลดเป็น ZIP แล้วแตกไฟล์
2. **ติดตั้ง Library** ผ่าน Terminal:
   ```bash
   pip install -r requirements.txt
